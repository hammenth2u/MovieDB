{% extends 'base.html.twig' %}

{% block title %}Movie{% endblock %}

{% block body %}
    <h1>Movie</h1>

    <table class="table">
        <tbody>
            {# <tr>
                <th>Id</th>
                <td>{{ movie.id }}</td>
            </tr> #}
            <tr>
                <th>Title</th>
                <td>{{ movie.title }}</td>
            </tr>
            {# <tr>
                <th>CreatedAt</th>
                <td>{{ movie.createdAt ? movie.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ movie.updatedAt ? movie.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr> #}
            <tr>
                <th>Image</th>
                <td>{% if movie.image %}
                    <img src="{{ asset(movie.image) }}" height="200">
                    {% else %}
                    Pas d'image
                    {% endif %}
                </td>
            </tr>
            Si on voulait afficher les genres il faudrait le faire ici !
            En faisant une boucle sur movie.genres
        </tbody>
    </table>

    <a href="{{ path('admin_movie_index') }}">back to list</a>

    <a href="{{ path('admin_movie_edit', {'id': movie.id}) }}">edit</a>

    {{ include('admin/movie/_delete_form.html.twig') }}
{% endblock %}
